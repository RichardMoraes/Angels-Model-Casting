# 🏛 Arquitetura do Sistema

> Visão detalhada da arquitetura técnica do Angels Model Casting

---

## 📊 Diagrama de Arquitetura

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENTE (Browser)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         Next.js App Router                           │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │                     Layout (RootLayout)                      │    │    │
│  │  │  - HTML/Body structure                                       │    │    │
│  │  │  - Global styles (globals.css)                               │    │    │
│  │  │  - Metadata configuration                                    │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  │                              │                                       │    │
│  │                              ▼                                       │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │                    Page (Home - "/")                         │    │    │
│  │  │  └─ CastingVitrine (Client Component)                        │    │    │
│  │  │      ├─ Header                                               │    │    │
│  │  │      ├─ AdvancedFilters                                      │    │    │
│  │  │      ├─ TalentCard[] (Grid)                                  │    │    │
│  │  │      ├─ Pagination                                           │    │    │
│  │  │      └─ TalentDetailSheet (Lazy)                             │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              SERVIDOR (Next.js)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────┐    ┌────────────────────┐    ┌──────────────────┐  │
│  │    API Routes      │    │   Static Assets    │    │    SSR/RSC       │  │
│  │                    │    │                    │    │                  │  │
│  │  /api/placeholder  │    │  /public/*         │    │  Layout.tsx      │  │
│  │  - SVG generator   │    │  - SVGs            │    │  Page.tsx        │  │
│  │                    │    │  - favicon         │    │                  │  │
│  └────────────────────┘    └────────────────────┘    └──────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           DADOS (Atualmente Mock)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │                        talentos.ts                                  │    │
│  │  - Array estático de TalentProfile                                  │    │
│  │  - Importado diretamente no CastingVitrine                          │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │                   Imagens Externas (CDN)                            │    │
│  │  - randomuser.me (avatares)                                         │    │
│  │  - picsum.photos                                                    │    │
│  │  - images.unsplash.com                                              │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Dados

### 1. Carregamento Inicial

```
Browser Request "/"
        │
        ▼
┌─────────────────┐
│   Next.js SSR   │
│  (layout.tsx)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    page.tsx     │
│  (Server Comp)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ CastingVitrine  │
│ (Client Comp)   │──────┐
└────────┬────────┘      │
         │               │
         ▼               │
┌─────────────────┐      │
│ Import talentos │◄─────┘
│   (Mock Data)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Render Initial  │
│     State       │
└─────────────────┘
```

### 2. Filtragem de Talentos

```
User Input (Search/Filter)
        │
        ▼
┌─────────────────────────────────────┐
│        Event Handlers               │
│  handleSearchChange(value)          │
│  handleGenderChange(value)          │
└────────────────┬────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│         useState Update             │
│  setSearchTerm(value)               │
│  setSelectedGender(value)           │
│  setCurrentPage(1) // Reset         │
└────────────────┬────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│     useMemo Recalculation           │
│                                     │
│  filteredTalents = talentos.filter( │
│    matchesSearch &&                 │
│    matchesGender &&                 │
│    matchesAgeRange                  │
│  )                                  │
└────────────────┬────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│       Pagination Slice              │
│                                     │
│  currentTalents = filteredTalents   │
│    .slice(startIndex, endIndex)     │
└────────────────┬────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│          Re-render                  │
│  - Update TalentCard[]              │
│  - Update Pagination                │
│  - Update Results Count             │
└─────────────────────────────────────┘
```

### 3. Visualização de Detalhes

```
User Click on TalentCard
        │
        ▼
┌─────────────────────────────────────┐
│    handleTalentClick(talent)        │
│                                     │
│  setSelectedTalent(talent)          │
│  setIsDetailSheetOpen(true)         │
└────────────────┬────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│      Suspense Boundary              │
│                                     │
│  ┌─────────────────────────────┐    │
│  │    Lazy Load                │    │
│  │  TalentDetailSheet          │    │
│  │  (First time only)          │    │
│  └─────────────────────────────┘    │
└────────────────┬────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│     TalentDetailSheet               │
│                                     │
│  ┌─────────────────────────────┐    │
│  │      CustomSidebar          │    │
│  │  - Animation In             │    │
│  │  - Overlay Render           │    │
│  │  - Body Scroll Lock         │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │      Content Tabs           │    │
│  │  - Summary                  │    │
│  │  - Photos                   │    │
│  │  - Videos                   │    │
│  │  - Skills                   │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

---

## 🧱 Camadas da Aplicação

### Layer 1: Apresentação (UI)

```
components/ui/           # Primitivos UI (shadcn/ui)
├── button.tsx          # Botões com variantes
├── card.tsx            # Containers de card
├── input.tsx           # Inputs de texto
├── select.tsx          # Dropdowns
├── tabs.tsx            # Sistema de abas
├── sheet.tsx           # Sidebars/Modals
└── badge.tsx           # Badges/Tags
```

### Layer 2: Componentes de Domínio

```
app/components/          # Componentes específicos do domínio
├── TalentCard.tsx      # Exibição de talento em card
├── TalentDetailSheet   # Detalhes completos
├── StarRating.tsx      # Avaliação por estrelas
├── SkillTag.tsx        # Tags de categorias
├── StatusIndicator.tsx # Indicadores de status
├── Header.tsx          # Navegação principal
├── Filters.tsx         # Filtros básicos
├── AdvancedFilters.tsx # Filtros avançados
├── Pagination.tsx      # Navegação de páginas
└── CustomSidebar.tsx   # Container sidebar
```

### Layer 3: Orquestração

```
app/components/
└── CastingVitrine.tsx   # Componente orquestrador
    - Gerencia estado global da página
    - Coordena filtros e resultados
    - Controla abertura/fechamento de detalhes
```

### Layer 4: Dados

```
app/data/
└── talentos.ts          # Fonte de dados (mock)

app/types/
└── TalentProfile.ts     # Definição de tipos
```

### Layer 5: Utilitários

```
lib/
└── utils.ts             # Funções utilitárias (cn)

app/api/
└── placeholder/         # API de imagens
```

---

## 🔧 Decisões de Arquitetura

### Por que Client Components?

A maioria dos componentes são **Client Components** (`"use client"`) porque:

1. **Interatividade**: Filtros, navegação de fotos, hover effects
2. **Estado Local**: Gerenciamento de filtros e seleções
3. **Event Handlers**: onClick, onChange, etc.
4. **Browser APIs**: window.innerWidth para responsividade

### Por que Não Redux/Zustand?

O estado atual é **localizado no CastingVitrine** porque:

1. **Escopo limitado**: Todo estado é usado em uma única página
2. **Prop drilling mínimo**: No máximo 2 níveis de profundidade
3. **Simplicidade**: useState + useCallback é suficiente
4. **Futuro**: Fácil migrar para Zustand se necessário

### Por que CustomSidebar ao invés de Sheet (Radix)?

```typescript
// CustomSidebar foi criada para:
// 1. Evitar problemas de DOM com Dialog/Sheet
// 2. Controle total sobre animações
// 3. Melhor performance (sem Portal)
// 4. Customização de comportamento (scroll lock, escape)
```

### Por que Lazy Loading do TalentDetailSheet?

```typescript
const TalentDetailSheet = lazy(() => import("./TalentDetailSheet"));

// Benefícios:
// 1. Bundle inicial menor
// 2. Carrega apenas quando necessário
// 3. Melhora LCP (Largest Contentful Paint)
```

### Por que memo() nos Componentes?

```typescript
const TalentCard = memo(function TalentCard({ talent, onClick }) {
  // ...
});

// Benefícios:
// 1. Evita re-render quando props não mudam
// 2. Importante para listas (map)
// 3. Combinado com useCallback para handlers
```

---

## 📈 Considerações de Performance

### Bundle Size

| Dependência | Tamanho Aprox. | Justificativa |
|-------------|----------------|---------------|
| React | ~40KB | Core |
| Next.js | ~90KB | Framework |
| Radix UI | ~15KB | Primitivos acessíveis |
| Lucide React | Tree-shaken | Apenas ícones usados |
| Framer Motion | ~30KB | Animações (pode otimizar) |
| Tailwind | Purged | Apenas classes usadas |

### Otimizações Implementadas

1. **Image Optimization**: Next/Image com lazy loading
2. **Priority Loading**: Primeira imagem com `priority`
3. **Code Splitting**: Lazy load de TalentDetailSheet
4. **Memoization**: memo, useMemo, useCallback
5. **CSS-only Animations**: Preferência sobre JS

### Métricas Alvo

| Métrica | Alvo | Status |
|---------|------|--------|
| LCP | < 2.5s | ✅ |
| FID | < 100ms | ✅ |
| CLS | < 0.1 | ✅ |
| Bundle (gzip) | < 200KB | ✅ |

---

## 🔮 Arquitetura Futura (Proposta)

### Com Backend Integration

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                 Frontend                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Next.js App Router                                                          │
│  ├── Server Components (RSC)                                                │
│  │   └── Data fetching inicial                                              │
│  ├── Client Components                                                       │
│  │   └── Interatividade                                                     │
│  └── TanStack Query                                                          │
│      └── Cache, revalidation, optimistic updates                            │
│                                                                              │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                  API                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Next.js API Routes                     Strapi v4                           │
│  ├── /api/auth/*                        ├── /api/talents                    │
│  ├── /api/revalidate                    ├── /api/categories                 │
│  └── Proxy/BFF                          └── /api/upload                     │
│                                                                              │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                               Database                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  PostgreSQL                             S3/Cloudinary                        │
│  └── Talentos, Users, etc.              └── Media storage                   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

*Última atualização: Dezembro 2024*

